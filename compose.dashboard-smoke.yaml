services:
  workoutreach-proxy:
    ports: !override
      - "127.0.0.1:8443:443"

  workoutreach-dashboard:
    environment:
      DASHBOARD_PUBLIC_ORIGIN: https://dashboard.workoutreach.localhost:8443

secrets:
  dashboard_password_hash:
    file: ./artifacts/dashboard-smoke/dashboard_password_hash
