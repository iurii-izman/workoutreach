# SBOM

Run `npm run sbom` to deterministically generate `sbom/workoutreach.cdx.json` from `package-lock.json` and the pinned container inventory. Review and commit the result after dependency changes.
