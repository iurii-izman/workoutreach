# Backup container placeholder

An encrypted backup target and key-management method have not been selected by the owner. No backup container is enabled in stages 0–1. See `docs/runbooks/backup-restore.md`; adding a transport or retention policy requires a reviewed ADR.
